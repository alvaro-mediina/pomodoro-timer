import { Card } from '../../models/utils-models'

interface Props {
    infoCards: Card[];
}

const InfoCardGrid = ({ infoCards } : Props) => {
    return(
        <div>
 
        </div>
    );
};

export default InfoCardGrid;